export const setName = (state, name) => {
    state.name = name;
}

export const setCity = (state, city) => {
    state.city = city;
}

export const setAge = (state, age) => {
    state.age = age;
}